apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose-windows-amd64.exe convert
    kompose.version: 1.31.2 (a92241f79)
  creationTimestamp: null
  labels:
    io.kompose.service: message-service-db
  name: message-service-db
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: message-service-db
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose-windows-amd64.exe convert
        kompose.version: 1.31.2 (a92241f79)
      creationTimestamp: null
      labels:
        io.kompose.network/compose-test-default: "true"
        io.kompose.service: message-service-db
    spec:
      containers:
        - env:
            - name: PGDATA
              value: /data/postgres
            - name: POSTGRES_DB
              value: message-db
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: blossom-secret
                  key: ROOT_PASSWORD
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: blossom-secret
                  key: ROOT_USER
          image: postgres:16.1
          name: message-service-db
          ports:
            - containerPort: 5432
              hostPort: 6387
              protocol: TCP
          resources: {}
          volumeMounts:
            - mountPath: /data/postgres
              name: message-service-db-claim0
      restartPolicy: Always
      volumes:
        - name: message-service-db-claim0
          persistentVolumeClaim:
            claimName: message-service-db-claim0
status: {}
